
/**
 * @swagger
 * components:
 *   schemas:
 *     Promotion:
 *       type: object
 *       properties:
 *         id:
 *           type: integer
 *           description: Identifiant unique de la promotion
 *         name:
 *           type: string
 *           description: Nom de la promotion
 *         description:
 *           type: string
 *           description: Description de la promotion
 *         startDate:
 *           type: string
 *           format: date-time
 *           description: Date de début de la promotion
 *         endDate:
 *           type: string
 *           format: date-time
 *           description: Date de fin de la promotion
 *         createdAt:
 *           type: string
 *           format: date-time
 *           description: Date de création de la promotion
 *         updatedAt:
 *           type: string
 *           format: date-time
 *           description: Date de dernière mise à jour de la promotion
 */