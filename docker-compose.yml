services:
  promotions:
    build: .
    container_name: promotions-service
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - backend
      
networks:
  backend:
    #external: true